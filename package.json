{
  "name": "brnt-toast",
  "version": "1.0.0",
  "scripts": {
    "build": "npm run clean && rm -rf docs/* &&  parcel build ./src/index.pug --public-url ./ --no-source-maps --dist-dir docs/",
    "dev": "parcel src/index.pug",
    "clean": "rm -rf .parcel-cache dist/"
  },
  "repository": {
    "type": "git"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@parcel/transformer-pug": "^2.3.2",
    "@parcel/transformer-sass": "^2.3.2",
    "autoprefixer": "^10.4.2",
    "parcel": "^2.3.2"
  }
}
